Kamu mengerjakan repo AINA web (Node.js/Express). Tugasmu: integrasikan muqarrar_chunks dari Supabase sebagai sumber jawaban kitab/PDF untuk AINA.
## Latar belakang
Di Supabase project yang sama, ada tabel `muqarrar_chunks` berisi halaman-halaman kitab Islam yang sudah di-OCR dan di-embed oleh sistem lain (AINA Scraper). RPC function `match_muqarrar_chunks` sudah dibuat dan siap dipakai.
- Embedding model: `text-embedding-3-large`, dimensions `1536` — SAMA dengan `knowledge_base`
- Supabase URL & SERVICE_ROLE_KEY: sama persis dengan yang sudah dipakai di project ini
Skema hasil RPC `match_muqarrar_chunks`:

kitab_id, kitab_name, author, description,
page_number, chapter, content, word_count, is_ocr, similarity

---
## Perubahan 1 — server.js: tambah fungsi fetchRelevantMuqarrar
Taruh tepat di bawah fungsi `fetchRelevantArticles`:
```javascript
/* ── Fetch relevant muqarrar/kitab chunks via pgvector ── */
async function fetchRelevantMuqarrar(userQuestion, intentPrimary) {
  const MUQARRAR_INTENTS = new Set(["fiqh", "factual", "question", "general"]);
  if (!MUQARRAR_INTENTS.has(intentPrimary)) return [];
  const supabase = getAdminClient();
  if (!supabase || !process.env.OPENAI_API_KEY) return [];
  try {
    const queryEmbedding = await generateEmbedding(userQuestion);
    const { data, error } = await supabase.rpc("match_muqarrar_chunks", {
      query_embedding: queryEmbedding,
      match_threshold: 0.35,
      match_count: 5,
    });
    if (error) {
      console.warn(`[Muqarrar] RPC error: ${error.message}`);
      return [];
    }
    const results = (data || []).filter(c => c.similarity > 0.35);
    if (results.length > 0) {
      console.log(`[Muqarrar] ✓ ${results.length} chunks ditemukan (top similarity=${results[0]?.similarity?.toFixed(3)}) — "${userQuestion.slice(0, 60)}"`);
    }
    return results;
  } catch (e) {
    console.warn(`[Muqarrar] fetch failed: ${e.message}`);
    return [];
  }
}

Perubahan 2 — server.js: tambah muqarrar ke Promise.all utama
Cari baris ini:

const [articles, pinnedUpdates, exchangeRates, dorarResult] = await Promise.all([
  resolveArticles(kbQuery, intent.primary),
  fetchPinnedUpdates(),
  isCurrencyQuery(retrievalQuery) ? fetchExchangeRates() : Promise.resolve(null),
  intent.primary === "fiqh" ? fetchDorarHadith(retrievalQuery) : Promise.resolve(null),
]);

Ganti dengan:

const [articles, pinnedUpdates, exchangeRates, dorarResult, muqarrarChunks] = await Promise.all([
  resolveArticles(kbQuery, intent.primary),
  fetchPinnedUpdates(),
  isCurrencyQuery(retrievalQuery) ? fetchExchangeRates() : Promise.resolve(null),
  intent.primary === "fiqh" ? fetchDorarHadith(retrievalQuery) : Promise.resolve(null),
  fetchRelevantMuqarrar(retrievalQuery, intent.primary),
]);

Perubahan 3 — server.js: build muqarrar context
Cari baris ini:

const knowledgeContext = buildKnowledgeContext(articles);

Tambahkan tepat di bawahnya:

const muqarrarContext  = buildMuqarrarContext(muqarrarChunks);
if (muqarrarChunks.length > 0) {
  console.log(`[Muqarrar] injected ${muqarrarChunks.length} chunks — Hal.${muqarrarChunks[0]?.page_number} ${muqarrarChunks[0]?.kitab_name}`);
}

Perubahan 4 — server.js: tambah muqarrarContext ke buildSystemPrompt
Di dalam call buildSystemPrompt({...}), tambahkan parameter muqarrarContext:

buildSystemPrompt({
  // ... semua parameter yang sudah ada ...
  knowledgeContext,
  muqarrarContext,    // ← tambah baris ini
  exchangeContext,
  // ... sisa parameter ...
})

Perubahan 5 — server.js: update import dari promptBuilder.js
Cari baris import dari promptBuilder.js (paling atas file), tambahkan buildMuqarrarContext:

import {
  buildKnowledgeContext,
  buildMuqarrarContext,    // ← tambah ini
  buildPinnedContext,
  // ... import lainnya yang sudah ada ...
} from "./engine/promptBuilder.js";

Perubahan 6 — engine/promptBuilder.js: tambah fungsi buildMuqarrarContext
Tambahkan fungsi baru ini setelah fungsi buildKnowledgeContext:

/**
 * Build muqarrar (kitab/PDF) context block for the system prompt.
 * Setiap fakta WAJIB disertai sitasi halaman.
 * @param {Array} chunks - Results from fetchRelevantMuqarrar()
 * @returns {string}
 */
export function buildMuqarrarContext(chunks) {
  if (!chunks || chunks.length === 0) return "";
  const chunksText = chunks.map(c => {
    const label = `[${c.kitab_name}${c.author ? ` — ${c.author}` : ""}, Hal. ${c.page_number}${c.chapter ? `, ${c.chapter}` : ""}]`;
    return `${label}\n${c.content}`;
  }).join("\n\n---\n\n");
  const rule =
    `⚠️ ATURAN WAJIB MUQARRAR: Setiap fakta dari kutipan kitab di bawah WAJIB disertai sitasi "(Hal. X — NamaKitab)". ` +
    `Jika ada teks Arab, kutip teks aslinya sebagai blockquote (>) lalu terjemahkan dan jelaskan. ` +
    `DILARANG menjelaskan tanpa menyebut sumber halamannya.`;
  return `\n\n---\n${rule}\n\n## Muqarrar / Kitab (Teks Sumber Primer)\n\n${chunksText}\n---`;
}

Perubahan 7 — engine/promptBuilder.js: update fungsi buildSystemPrompt
Di fungsi buildSystemPrompt, tambahkan parameter muqarrarContext:

Cari destructuring parameter buildSystemPrompt, tambahkan muqarrarContext = "":

export function buildSystemPrompt({
  // ... parameter yang sudah ada ...
  knowledgeContext,
  muqarrarContext = "",    // ← tambah ini
  exchangeContext,
  // ...
})

Lalu cari baris di dalam fungsi dimana semua context digabung ke string prompt (biasanya ada ${knowledgeContext}), tambahkan ${muqarrarContext} tepat setelahnya:

// sebelum:
${knowledgeContext}${exchangeContext}...
// sesudah:
${knowledgeContext}${muqarrarContext}${exchangeContext}...

Catatan penting
Tidak ada perubahan database — RPC match_muqarrar_chunks sudah dibuat di Supabase oleh AINA Scraper. Pastikan SUPABASE_URL dan SUPABASE_SERVICE_ROLE_KEY di .env adalah project Supabase yang SAMA.

muqarrarChunks bisa array kosong — aman, buildMuqarrarContext([]) return "" dan tidak inject apapun ke prompt.

Threshold 0.35 (lebih rendah dari KB 0.40) karena teks kitab Arab lebih distinctive semantically.

Setelah integrasi berhasil, ketika user tanya soal fiqh/kitab, AINA akan otomatis menjawab dengan referensi seperti: "Berdasarkan Fathul Qarib, Hal. 47 (Bab Thaharah)..."
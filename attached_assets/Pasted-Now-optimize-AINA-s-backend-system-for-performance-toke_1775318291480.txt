Now optimize AINA’s backend system for performance, token efficiency, and long-session stability.

Focus ONLY on:
- token usage efficiency
- chat history management
- model routing
- response speed
- system stability

Do NOT modify the answer style/format rules from previous steps.

========================
MAIN GOALS
========================

1. Prevent slowdowns in long chat sessions
2. Reduce token waste significantly
3. Maintain good answer quality with lower cost
4. Keep system stable even after many interactions

========================
TASK
========================

A. CHAT HISTORY CONTROL (CRITICAL)

Problem:
Conversation history grows too large → slows response & increases token cost

Solution:
Implement hybrid history system:

1. Keep only:
   - Last 4–6 messages (full detail)

2. Older messages:
   - Summarize into compact context

3. Create function:
   summarizeConversation(history)

Output:
- Short, structured summary of previous conversation
- Max ~500–800 tokens

4. Inject into prompt as:
"Previous context summary"

Goal:
Reduce token size while preserving meaning

========================
B. TOKEN OPTIMIZATION

Audit ALL prompt contributors:
- system prompt
- KB injection
- web results
- history
- formatting rules

Tasks:
1. Remove duplicated instructions
2. Compress repeated rules
3. Limit KB injection (~1500–2000 chars max)
4. Limit web results (~800–1200 chars)
5. Ensure total prompt stays efficient

Goal:
Lean but still accurate

========================
C. MODEL ROUTING OPTIMIZATION

Fix routing logic:

1. LIGHT QUESTIONS
(simple factual, short answers)
→ use lightweight model

2. KB-STRONG QUESTIONS
→ prioritize KB + lightweight model

3. DYNAMIC / REAL-TIME QUESTIONS
("sekarang", "terbaru", "siapa presiden")
→ MUST use web source + stronger model

4. COMPLEX QUESTIONS
→ use stronger model

Ensure fallback is DIFFERENT model (not same one)

========================
D. FAIL-SAFE FOR LONG SESSIONS

If:
- history too long
- prompt too large

Then:
- auto compress context
- drop low-priority history
- keep system responsive

Never allow:
- request hanging
- no response

========================
E. DEBUG MODE (IMPORTANT)

Add internal debug logs:

Track:
- total prompt size (tokens estimate)
- history length
- KB size
- web content size
- model used
- whether summarization triggered

Make it optional:
debug: true/false

========================
F. RESPONSE TIME IMPROVEMENT

1. Reduce unnecessary processing steps
2. Avoid multiple AI calls unless needed
3. Prefer single efficient pipeline
4. Set reasonable timeout fallback

========================
IMPLEMENTATION DETAILS
========================

- Refactor chat pipeline if needed
- Create reusable utilities:
  - summarizeConversation()
  - optimizePromptContext()
  - routeModel()

- Apply optimization BEFORE sending to model

========================
OUTPUT REQUIREMENTS
========================

After implementing:

1. Show changed files
2. Explain main performance issues before
3. Explain new system flow
4. Show before vs after:
   - prompt size
   - response speed (approx)
5. Simulate:
   - 1 message
   - 10 messages
   - 20 messages session

IMPORTANT:
Do not just suggest ideas.
Actually implement the optimization logic in code.